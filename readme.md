
```
python3 -m venv venv
source venv/bin/active 
# активируем виртаульную среду (это код для линухи, для винды и мака мб по другому)
pip3 install -r requirements.txt
# если у вас vscode то модно в нем выбрать в ipynb интерпретатор питоона, который лежит в папке venv и работать
```